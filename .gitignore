venv/
__pycache__/
*_pb2.py
aes.key
key.aes
ca.key
ca.crt
ca.der
server.key
server.crt
server.log
sense_data.jsonl
sense_fs_backup/
.DS_Store

# A firmware built with KITSUNE_DEV_DOMAIN carries your own hostnames. It is a
# build artifact, not a reference, and the reference (out/kitsune.bin) is
# committed alongside it.
sense/firmware/kitsune-4513/out/kitsune-custom-*
