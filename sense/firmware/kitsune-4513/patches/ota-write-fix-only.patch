diff --git a/kitsune/fatfs_cmd.c b/kitsune/fatfs_cmd.c
index 2ad66c37..3c2ac058 100644
--- a/kitsune/fatfs_cmd.c
+++ b/kitsune/fatfs_cmd.c
@@ -1233,7 +1233,9 @@ static _i32 _WriteBootInfo(sBootInfo_t *psBootInfo)
     unsigned long ulBootInfoCreateFlag;
 
 
-    sl_FsDel((unsigned char *)IMG_BOOT_INFO,ulBootInfoToken);
+    /* v3: no delete. The record is a fail-safe file; updating it in
+       place lets the FS keep the previous version until the new one
+       commits. Hello's later master also dropped this delete. */
     //
     // Initialize boot info file create flag
     //
@@ -1262,11 +1264,11 @@ static _i32 _WriteBootInfo(sBootInfo_t *psBootInfo)
             sl_FsWrite(hndl, 0, (_u8 *)psBootInfo, sizeof(sBootInfo_t));  // Dummy write, we don't care about the result
         }
 	}
-	if( 0 < sl_FsWrite(hndl, 0, (_u8 *)psBootInfo, sizeof(sBootInfo_t)) )
 	{
-		LOGI("WriteBootInfo: ucActiveImg=%d, ulImgStatus=0x%x\n\r", psBootInfo->ucActiveImg, psBootInfo->ulImgStatus);
+		_i32 wrv = sl_FsWrite(hndl, 0, (_u8 *)psBootInfo, sizeof(sBootInfo_t));
+		_i32 crv = sl_FsClose(hndl, 0, 0, 0);
+		LOGI("WriteBootInfo: ucActiveImg=%d, ulImgStatus=0x%x w %d c %d\n\r", psBootInfo->ucActiveImg, psBootInfo->ulImgStatus, wrv, crv);
 	}
-	sl_FsClose(hndl, 0, 0, 0);
     return 0;
 }
 
@@ -1364,6 +1366,17 @@ void boot_commit_ota() {
 								IMG_ACT_USER1;
         _WriteBootInfo(&sBootInfo);
 		//send_top("rst ", sizeof("rst "));
+		{
+			sBootInfo_t bi_chk;
+			_i32 bi_rv;
+			vTaskDelay(1000);
+			memset(&bi_chk, 0, sizeof(bi_chk));
+			bi_rv = _ReadBootInfo(&bi_chk);
+			LOGI("armchk rd %d st %x img %d\n", bi_rv, bi_chk.ulImgStatus, bi_chk.ucActiveImg);
+			bi_rv = sl_Stop(30000);
+			LOGI("slstop rv %d\n", bi_rv);
+			vTaskDelay(500);
+		}
 		mcu_reset();
 	}
 }
@@ -1373,6 +1386,17 @@ void reset_to_factory_fw() {
 	sBootInfo.ulImgStatus = IMG_STATUS_NOTEST;
 	sBootInfo.ucActiveImg = IMG_ACT_FACTORY;
 	_WriteBootInfo(&sBootInfo);
+	{
+		sBootInfo_t bi_chk;
+		_i32 bi_rv;
+		vTaskDelay(1000);
+		memset(&bi_chk, 0, sizeof(bi_chk));
+		bi_rv = _ReadBootInfo(&bi_chk);
+		LOGI("armchk rd %d st %x img %d\n", bi_rv, bi_chk.ulImgStatus, bi_chk.ucActiveImg);
+		bi_rv = sl_Stop(30000);
+		LOGI("slstop rv %d\n", bi_rv);
+		vTaskDelay(500);
+	}
 	mcu_reset();
 }
 
@@ -1603,6 +1627,17 @@ void file_download_task( void * params ) {
                     memcpy(sBootInfo.sha[_McuImageGetNewIndex()], download_info.sha1.bytes, SHA1_SIZE );
                     //sBootInfo.ucActiveImg this is set by boot loader
                     _WriteBootInfo(&sBootInfo);
+                    {
+                    	sBootInfo_t bi_chk;
+                    	_i32 bi_rv;
+                    	vTaskDelay(1000);
+                    	memset(&bi_chk, 0, sizeof(bi_chk));
+                    	bi_rv = _ReadBootInfo(&bi_chk);
+                    	LOGI("armchk rd %d st %x img %d\n", bi_rv, bi_chk.ulImgStatus, bi_chk.ucActiveImg);
+                    	bi_rv = sl_Stop(30000);
+                    	LOGI("slstop rv %d\n", bi_rv);
+                    	vTaskDelay(500);
+                    }
                     mcu_reset();
                 }
             } else {
